//we don't load maps at runtime like /tg/ / Aurora.
//Maybe we'll do that at some point, but until then, if we want to use submaps on Nerva/Glloydstation, we have to get a little funky
//these are submap templates, they are spawned by a submap template_loader.
//define these in a file per-map. i.e. nerva_submaps.dm
//see the mapping docs for more info.

/datum/map_template/submap
